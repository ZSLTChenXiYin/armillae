---
armillae-core: "minor:feat"
---

Add explicit native strict Schema and JSON Object with client Schema validation modes to the public completion protocol.

Preserve the legacy output formats and schema/history round trips. Unknown enforcement modes are rejected instead of silently downgraded.
